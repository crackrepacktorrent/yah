/** The Better Auth organization slug for the shared admin application. */
export const ORG_SLUG = 'yah';

/** Serializes Better Auth startup migrations and one-time auth maintenance. */
export const AUTH_MAINTENANCE_LOCK_ID = 941_741;
