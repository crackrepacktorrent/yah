export { AUTH_MAINTENANCE_LOCK_ID, ORG_SLUG } from '@yah/admin-core/constants';

/** Brand orange used for the logo on auth pages (hex required — CSS vars don't work as SVG fill props). */
export const LOGO_FILL_ORANGE = '#ff6f00';
